spring:
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}
      connect-timeout: ${REDIS_CONNECT_TIMEOUT_MILLIS}ms
      timeout: ${REDIS_TIMEOUT_MILLIS}ms
