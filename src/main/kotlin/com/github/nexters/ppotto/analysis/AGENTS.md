<!-- Parent: ../AGENTS.md -->

# analysis

Analysis domain. Owns both `Analysis` and `Photo` — a photo only ever exists as part of an analysis (`Analysis : Photo = 1:N` via `photos.analysis_id`), so they share one domain package instead of being split into `analysis`/`photo`. No DB-level FK constraints (same reasoning as `board/`). The 20–100 photo group count validation (each group 1–10 photos, `MIN_GROUP_COUNT`/`MAX_GROUP_COUNT`/`MAX_BURST_GROUP_SIZE`) is implemented in the `init` of `CreateAnalysisCommand` and `PhotoUploadGroupRequest` (returns 400 `ANALYSIS-001`/`ANALYSIS-010`/`ANALYSIS-009`), so an invalid request never reaches `AnalysisService`. The one-active-analysis-per-user limit is enforced via service-level pre-check and a partial unique index `uk_analysis_active` on `analysis(user_id) WHERE status IN (UPLOADING, ANALYZING)` (returns 409 `ANALYSIS-002`). The pre-check is deliberately kept on top of the constraint: it runs inside the board row lock that `createAnalysis` already holds, so the common case answers 409 without burning an insert and a rolled-back transaction, while the constraint stays as the race-proof fallback. Daily quota is intentionally not implemented yet — see Rules below.

| Directory | Description |
|-----------|---------|
| `config/PixianProperties.kt` | `@ConfigurationProperties("pixian")` + `@Validated`. API id/secret, test mode, and remove-background URI bound from `PIXIAN_*` env vars |
| `config/PixianConfig.kt` | `@ImportHttpServices(group = "pixian")` registration of `PixianApi`; the shared `HttpServiceClientConfig` pins it to the JDK HttpClient |
| `config/VertexAiProperties.kt` | `@ConfigurationProperties("vertexai")` + `@Validated`. Project/location plus separate classification and verification timeout budgets bound from `VERTEX_AI_*` env vars; verification has its own shorter timeout because it is a quality guard with classification fallback |
| `config/AnalysisPipelineProperties.kt` | `@ConfigurationProperties("analysis.pipeline")` + `@Validated`. `max-concurrent-runs` (동시에 돌 수 있는 파이프라인 수) 와 `resume-limit` (부팅 시 한 번에 재개할 최대 건수) 를 `ANALYSIS_PIPELINE_*` 환경변수에서 바인딩 |
| `config/AnalysisPipelineProperties.kt` | `@ConfigurationProperties("analysis.pipeline")` + `@Validated`. `max-concurrent-runs`(동시에 돌 수 있는 파이프라인 수)와 `resume-limit`(부팅 시 한 번에 재개할 최대 건수)를 `ANALYSIS_PIPELINE_*` 환경변수에서 바인딩 |
| `config/VertexAiConfig.kt` | Defines the `google-genai` `Client` bean in Vertex AI mode from `VertexAiProperties`, authenticating with the same service-account key file as `GcsConfig` (`gcsProperties.credentialsPath`) scoped to `cloud-platform`. These four live here, not in `global/config`, because analysis is their only consumer |
| `domain/Analysis.kt` | 순수 Kotlin 분석 모델. `AnalysisId`·`UserId`·`BoardId`, 상태·진행률·시각과 nullable `failedCode: AnalysisErrorCode?`·`failedReason`을 보관한다. `requireUploading()`은 업로드·시작·URL 재발급이 공유하는 상태 검사이며 위반 시 `ANALYSIS-003`을 반환한다. |
| `domain/AnalysisStatus.kt` | `UPLOADING` / `ANALYZING` / `COMPLETED` / `FAILED` — pure Kotlin enum, not the jOOQ-generated type. `companion object ACTIVE` defines the set of active states (`UPLOADING`, `ANALYZING`) used in service-level validation and DB partial unique index. |
| `domain/Photo.kt` | Pure Kotlin `data class`: `id: PhotoId`, `analysisId: AnalysisId`, `boardId: BoardId`, `contentType: PhotoContentType`, `uploadStatus`, `uploadedAt`, `takenAt`, `burstGroupId`, `isRepresentative`, `createdAt`, `updatedAt` |
| `domain/UploadStatus.kt` | `PENDING` / `COMPLETED` / `FAILED` |
| `domain/PhotoContentType.kt` | Stored photo content-type enum: `JPEG`/`PNG`/`HEIC`/`WEBP`, each carrying `mimeType` and `extension`. `HEIC` remains for existing rows and object-key mapping, but new upload requests use presentation `PhotoUploadContentType` and only accept JPEG/PNG/WEBP because sticker cutout sends the original source image to Pixian, which does not support HEIC input. |
| `domain/PhotoStorage.kt` | Outbound port stated as intent, never as storage layout: `issueUploadUrls(photos): Map<PhotoId, String>`, `issueReadUrls(photos): Map<PhotoId, String>`, `sourceUri(photo)` (the reference handed to Gemini and to sticker cutout), `uploadedObjects(analysisId, photos): Map<PhotoId, BlobMeta>` (`BlobMeta(size, createdAt)` carries the object's real size/creation time so the caller need not re-derive `uploaded_at` or trust bare key existence), and `deleteAll(analysisId): Int`. Object keys, the bucket name and the `gs://` scheme live only inside `infrastructure/GcsPhotoStorage`, so no application-layer file imports `PhotoObjectKeys` or `GcsProperties`. No Spring/GCS SDK imports |
| `domain/ThemeClassifier.kt` | Outbound port for theme classification, sticker regeneration and sticker-subject verification. Named by intent, not by vendor — the Vertex AI adapter is `infrastructure/VertexAiGeminiClassifier` |
| `domain/SourcePhotoImageReader.kt` | Outbound port that reads the original photo bytes behind a `PhotoRef.sourceUri`. Kept here with the other outbound ports rather than in the adapter package; implemented by `infrastructure/GcsSourcePhotoImageReader` |
| `domain/StickerBackgroundRemover.kt` | Outbound port for sticker background removal, implemented by `infrastructure/PixianBackgroundRemover` |
| `domain/StickerStorage.kt` | Outbound port that uploads generated sticker PNG bytes under a caller-chosen object key. Returns `Unit` — the caller already owns the key |
| `domain/PhotoRef.kt` | `photoId: PhotoId`, `sourceUri` (technology-neutral name; only the GCS adapters produce and consume the `gs://` form), `mimeType`, `burstGroupId`, `isRepresentative` |
| `domain/AnalysisErrorCode.kt` | 기존 `001~011`을 유지한다. 비동기 실패를 `012`(전체 피사체 없음), `013`(전체 생성 실패), `014`(결과 저장), `015`(내부 오류·만료), `016`(취소), `017`(분류 호출)로 구분한다. 분류 응답 오류는 기존 `007`, 재생성 API의 이미지 처리 실패는 기존 `011`이다. `code`의 `@get:JsonValue`로 enum을 `ANALYSIS-xxx` 문자열로 직렬화한다. `006`은 문서 예약이며 미구현이다. |
| `domain/AnalysisDiscardedEvent.kt` | `data class AnalysisDiscardedEvent(analysisId: AnalysisId)` — 사용자 취소 또는 서버 만료 시 `AnalysisDiscardService`가 발행하며, 커밋 후 `AnalysisCleanupEventListener`가 사진 오브젝트를 정리한다. |
| `infrastructure/PhotoObjectKeys.kt` | `object` (stateless, no DI needed) wrapping the shared `global/storage/ObjectKeyGenerator` with this domain's own convention: namespace `"photos"`, prefix `photos/{analysisId}/`, key `photos/{analysisId}/{photoId}.{PhotoContentType.extension}`. The `analysisId` segment lets `GcsPhotoStorage.uploadedObjects` list all of one analysis's objects in a single GCS call. Originally lived in `domain/` as a `@Component`, which violated this domain's "no Spring imports" rule — moved here and converted to a plain `object` since it has no state to inject |
| `infrastructure/AnalysisRepository.kt` | typed id 기반 jOOQ 저장·조회·행 잠금. `markFailed(id, reason, failedCode)`는 활성 상태만 실패로 전환하고 코드 문자열을 저장한다. 진행률 갱신의 `SET LOCAL lock_timeout`은 리스너의 `REQUIRES_NEW` 트랜잭션 안에서만 실행한다. `toDomain`이 nullable DB 코드를 enum으로 복원한다. |
| `infrastructure/AnalysisWithdrawalRepository.kt` | Withdrawn-user analysis id lookup and hard deletion, kept out of `AnalysisRepository` so the serving path never exposes a user-wide delete |
| `infrastructure/integration/BoardAnalysisActivityAdapter.kt` | Production `BoardAnalysisActivityPort` adapter through `AnalysisQueryService` |
| `infrastructure/PhotoRepository.kt` | Typed-id signatures throughout. `saveAll(analysisId, boardId, items)` — single multi-row `INSERT ... RETURNING`, relies on Postgres preserving input order for plain multi-VALUES inserts so the response can zip photos back to the request order. `findPendingByAnalysisId()`, `findAllByAnalysisId()`, `markCompletedBatch(updates)`, and `markFailedBatch(ids)` support upload verification; both batch writes return the affected row count instead of re-reading rows, `markCompletedBatch` sends one JDBC batch (each row carries its own `uploaded_at`) and `markFailedBatch` is a single `UPDATE ... WHERE id IN (...)`, so verifying 100 photos no longer costs 100 round trips inside the lock transaction. `findCompletedByIds(analysisId, boardId, ids)` and `countOwnedByAnalysis(analysisId, boardId, ids)` back the cross-domain read and ownership checks; both deduplicate ids, short-circuit empty input without a query, scope every match to the owning analysis and board at the query level (no unscoped by-id photo lookup exists, so a second caller cannot turn it into an IDOR), and only accept `upload_status = COMPLETED` so PENDING/FAILED photos (no GCS object) never pass ownership or get a signed URL. `markAllFailedByAnalysisId(analysisId)` is an unconditional bulk `FAILED` transition for every photo of an analysis, used by `cancelAnalysis` (unlike `markFailedBatch`, it is not scoped to `PENDING` ids since a cancel must close out every photo row regardless of current status). Completed photos store the GCS object's real creation time in `uploaded_at`; missing or 0-byte pending photos are marked `FAILED` when analysis starts. `toDomain()`'s DB value mapping: `content_type` is mapped via `PhotoContentType.entries.find + error(...)` (not `fromMimeType`, which is for request validation and throws 400) — DB inconsistency is a server problem (500), not client input error (400) |
| `infrastructure/GcsPhotoStorage.kt` | GCS SDK implementation of `PhotoStorage`, and the only place that knows photo object keys, the bucket name and the `gs://` scheme. `issueUploadUrls` loops internally (GCS V4 signing has no batch API) but is called once per request from the service; each signed URL also signs `x-goog-content-length-range: 0,{MAX_PHOTO_SIZE_BYTES}` (15MB, per spec) as an ext header, so GCS rejects oversized PUT bodies with 400 — the client must send the identical header value or GCS returns 403 `SignatureDoesNotMatch` instead, and a web client additionally needs this header allow-listed in the bucket's CORS config (both are tracked outside this repo, not yet done). `uploadedObjects` uses `Storage.list(bucket, prefix(...))` — one GCS call instead of one `get()` per photo — and maps each `Blob` to `BlobMeta(size, createTimeOffsetDateTime)` keyed by `PhotoId`, so callers never see object keys. `issueReadUrls` delegates to the shared `global/storage/GcsReadUrlIssuer` instead of signing GET URLs itself |
| `infrastructure/StickerObjectKeys.kt` | `object` wrapping `global/storage/ObjectKeyGenerator` for generated sticker images. Initial analysis output uses `stickers/{analysisId}/{themeIndex}-{sourcePhotoId}.png`; recap regeneration uses `stickers/{stickerId}/{sourcePhotoId}-{regenerationId}.png`, grouping replacement images under the sticker id while keeping each regeneration upload unique. These bare keys are what the sticker domain must persist in `stickers.image_key`, because `sticker/infrastructure/GcsStickerImageStorage` signs them as `BlobId` object names |
| `infrastructure/GcsStickerStorage.kt` | Write side of generated sticker images: `StickerStorage` adapter that uploads PNG bytes to the shared bucket. Returns `Unit` — the caller passed the key in. Read URL signing is not its job — the sticker domain issues those through `StickerImageStoragePort` |
| `infrastructure/VertexAiGeminiClassifier.kt` | Gemini `ThemeClassifier` 구현. 초기 분류 호출 실패는 `017`, 응답 파싱·분류 검증 실패는 `007`이다. 짧은 사진 alias를 도메인 id로 복원하고 유효하지 않은 alias·테마를 제외한다. `repairCrossThemeDuplicates`로 테마 간 사진 중복을 보정한 뒤 `ThemeClassificationValidator`로 빈 결과까지 항상 검증하며, 보정한 `removedPhotoCount`는 WARN으로 기록한다. `mainColor`·코멘트 오류는 기존 기본값·제외 정책을 유지한다. `cappedText`가 뱃지·요약 길이를 저장 한계로 정규화한다. 카피 톤 `systemInstruction`(`GeminiPrompts.COPY_VOICE`)은 **분류 호출에만** 붙인다 — 재생성·재확인 호출은 사용자 노출 텍스트를 만들지 않고, 톤 지시가 `targetSubject`에 새면 컷아웃 도구가 분리할 수 없는 서술이 나온다. 피사체 검증은 짧은 별도 시간 제한을 사용하며, 명시적 `subjectPresent=false`만 피사체 없음이다. 판정 누락이나 `true`인데 대상이 비어 있는 응답은 예외를 발생시켜 파이프라인이 최초 분류값으로 복구하도록 한다. |
| `infrastructure/VertexAiGeminiSchemas.kt` | Vertex AI structured-output schemas for classification, sticker regeneration, and sticker-subject verification, held as immutable `val`s (`CLASSIFICATION_RESPONSE_SCHEMA`, `STICKER_RESPONSE_SCHEMA`, `VERIFICATION_RESPONSE_SCHEMA`) rather than rebuilt per call. Kept separate from `VertexAiGeminiClassifier` so the adapter's companion object only owns response-to-domain mapping. 스키마는 `GeminiSchemaDsl.kt`의 타입 DSL로 선언한다 — 필드를 `GeminiThemeResponse::observedDetails` 같은 프로퍼티 참조로 적으므로 응답 DTO 이름이 바뀌면 스키마가 따라오고, **선언 순서가 곧 `propertyOrdering`, 기본값이 곧 `required`** 라 같은 필드명을 세 목록에 적던 표류가 사라진다. DTO에 있는데 선언되지 않은 필드와 두 번 선언한 필드는 스키마 생성 시점에 `IllegalArgumentException`으로 터진다 — 전자는 지금까지 조용히 null로 파싱되던 버그다. 모든 필드가 `description`을 갖는다 — 구조화 출력에서 description은 그 필드 토큰이 디코딩되기 직전에 읽히는 프롬프트 채널이라, 길이·톤 제약을 프롬프트 본문보다 가까운 자리에 둔다. `stickerSchema(sourcePhotoIdDescription)`로 분류용과 재생성용을 나눈 이유도 같다: 분류는 "이 테마의 categorizedPhotoIds에서만", 재생성은 "전체 alias 목록에서"로 정답이 다르다. `themeSchema`의 첫 속성 `observedDetails`는 도메인으로 내려가지 않는 스크래치 필드다 — `propertyOrdering` 덕에 `theme`·`recap` 토큰보다 먼저 디코딩되므로, 카피가 무색 형용사 대신 사진에서 관찰한 구체 명사를 조건으로 삼게 하는 공짜 CoT다. 순서가 뒤집히면 효과가 조용히 0이 되므로 테스트가 첫 속성을 단언한다. 응답은 `GeminiResponseSpans`가 Sentry 스팬에 통째로 기록하므로 별도 로깅을 추가하지 않는다 |
| `infrastructure/SourcePhotoStickerGenerator.kt` | `StickerGenerator` adapter that turns the already-selected source photo into a sticker without Gemini image generation. Reads the original image bytes through `SourcePhotoImageReader`, sends those bytes to `StickerBackgroundRemover`, trims transparent padding with `StickerImageCropper`, and returns PNG bytes for upload |
| `infrastructure/GcsSourcePhotoImageReader.kt` | GCS implementation of `SourcePhotoImageReader`. Parses the `gs://{bucket}/{objectKey}` URI carried by `PhotoRef.sourceUri` and reads the original object bytes through the shared `Storage` bean. Raises no domain exception: `StorageException` propagates as-is and a malformed URI fails `require` (a bug, not a client error); 초기 파이프라인은 테마별 생성 실패로 보존하고 재생성 API는 `ANALYSIS-011`로 변환한다 |
| `infrastructure/StickerImageCropper.kt` | PNG post-processor for generated stickers. Finds a two-stage alpha-channel bounding box: first the solid subject core, then weak alpha detail only around that core, so distant background-removal residue does not expand the crop while nearby translucent details stay included. Keeps 12% padding based on the final subject's longer side, clamps it to the source image bounds, and re-encodes the cropped transparent PNG. Invalid or fully transparent PNGs fail with `IOException`; 초기 파이프라인은 테마별 생성 실패로 보존하고 재생성 API는 `ANALYSIS-011`로 변환한다 |
| `infrastructure/PixianApi.kt` | Spring HTTP service interface for Pixian background removal, registered by `PixianConfig` in the analysis infrastructure package so `global` does not depend on this domain |
| `infrastructure/PixianBackgroundRemover.kt` | Pixian-backed `StickerBackgroundRemover`. Sends original source-photo image bytes to Pixian with Basic auth, requests PNG output, logs charged credits, and retries exactly once on a 5xx or transport failure through `global/retry`'s `retrying(RetryPolicy(maxAttempts = 2), ...) { }.getOrThrow()` — `isRetryable` is the retry predicate, so a 4xx is never retried. Raises no domain exception: `RestClientException`/`RestClientResponseException` propagate and 초기 파이프라인은 테마별 생성 실패로 보존하고 재생성 API는 `ANALYSIS-011`로 변환한다 |
| `infrastructure/integration/StickerAnalysisPhotoOwnershipAdapter.kt` | Production sticker `AnalysisPhotoOwnershipPort` adapter through `AnalysisQueryService.ownsAnalysisPhotos` |
| `infrastructure/integration/StickerRecapPhotoAdapter.kt` | Production sticker `RecapPhotoQueryPort` adapter mapping `PhotoQueryService.getReadablePhotos` to recap photo metadata |
| `infrastructure/integration/AnalysisStickerRegenerationAdapter.kt` | Production sticker `StickerRegenerationPort` adapter. Resolves the sticker's recap photos through `PhotoQueryService.getPhotoRefs`, logs and skips any non-completed/missing photo rows, returns `null` when no usable photo remains (the sticker service turns that into `STICKER-006`), then asks Gemini for a new source/subject, turns that source photo into a Pixian cutout PNG through `StickerGenerator`, and uploads it through `StickerStorage`. As the provider-side adapter of a sticker port it owns the analysis domain's failure policy for this path, so it maps any cutout/upload failure to `ANALYSIS-011` |
| `infrastructure/integration/WithdrawnUserAnalysisDeletionAdapter.kt` | Production user `WithdrawnUserAnalysisDeletionPort` adapter through `AnalysisWithdrawalService` |
| `application/AnalysisService.kt` | Authenticated create and upload-verification/start use cases, all on typed ids; enforces row-locked board ownership and analysis ownership before persistence or storage access. `createAnalysis(userId, boardId, CreateAnalysisCommand)` keeps only DB work inside `transactionTemplate.execute` (board row lock, active-analysis pre-check, analysis + photo insert) and signs the 20–100 upload URLs **after** that transaction commits, so V4 RSA signing never holds the board lock or a pooled connection; if signing fails the client recovers through `reissue`. `startUpload` completes uploaded pending photos, marks missing or 0-byte pending photos as `FAILED`, transitions the analysis to `ANALYZING`, and publishes `AnalysisStartRequestedEvent`. `reissueUploadUrls(userId, analysisId)` shares `startUpload`'s `findByIdAndUserId` → `requireUploading` skeleton (unlocked read, no `@Transactional` — it performs no writes), then re-signs upload URLs only for currently `PENDING` photos through the same `issueUploadUrlItems` helper `createAnalysis` uses; returns an empty list if nothing is pending. `cancelAnalysis(userId, analysisId)` row-locks the analysis (`findByIdForUpdate`; missing or owned by a different user → `ANALYSIS-005`, checked inline rather than via a dedicated repository method), requires `UPLOADING` (else `ANALYSIS-004`), then hands the analysis to `AnalysisDiscardService.discard(analysisId, FAILED_REASON_CANCELED, ANALYSIS_CANCELED)` inside the same `@Transactional`. The file holds no logger: request-shape validation moved to `CreateAnalysisCommand`/`PhotoUploadGroupRequest` and the state guard to `Analysis.requireUploading()`, so every method body is business steps only |
| `application/AnalysisDiscardService.kt` | 사용자 취소와 서버 만료가 공유하는 `discard(analysisId, failedReason, failedCode)`. `markFailed` → 사진 `FAILED` 처리 → `AnalysisDiscardedEvent`를 호출자 트랜잭션 안에서 실행한다(`MANDATORY`). 분석이 실제로 전환되지 않으면 `false`를 반환하고 사진·이벤트에 부수효과를 남기지 않는다. 취소는 `016/CANCELED`, 만료는 `015/EXPIRED`를 저장한다. |
| `application/StaleAnalysisCleanupService.kt` | `updated_at`이 기준 시각보다 오래된 `UPLOADING`/`ANALYZING`을 찾아 `AnalysisDiscardService`에 `015/EXPIRED`로 넘긴다. 대상 행 잠금부터 분석·사진 상태 전환까지 배치 하나를 한 트랜잭션으로 처리한다. |
| `infrastructure/AnalysisCleanupRepository.kt` | 멈춘 분석 조회 전용 리포지토리 — 만료 대상 `findStaleActiveIds`(`updated_at` 기준의 활성 행을 `FOR UPDATE SKIP LOCKED`로 잠가 중복 배치와 진행 중인 결과 저장을 건너뛴다)와 재개 대상 `findResumableAnalyzing`. `AnalysisRepository`의 함수 수 상한 때문에 분리했다. |
| `application/OrphanedAnalysisResumeService.kt` | `ApplicationReadyEvent` 리스너. 재시작으로 끊겨 `ANALYZING`으로 남은 분석을 찾아 `AnalysisStartRequestedEvent`를 다시 발행한다. 업로드 완료 사진이 하나도 없으면 재개 대신 `NOT_RESUMABLE`로 즉시 `FAILED`. 만료 시한(`analysis.stale-cleanup.timeout-minutes`)을 이미 넘긴 행은 건드리지 않고 만료 배치에 넘긴다 |
| `infrastructure/StaleAnalysisCleanupScheduler.kt` | `analysis.stale-cleanup.enabled=true` 일 때만 뜨는 크론 스케줄러. 만료된 분석이 있을 때만 WARN 한 줄을 남긴다 |
| `infrastructure/StaleAnalysisCleanupProperties.kt` | `@ConfigurationProperties("analysis.stale-cleanup")` + `@Validated`. enabled / timeout-minutes / batch-size / cron |
| `domain/RepairedThemeClassifications.kt` | `List<ThemeClassification>.repairCrossThemeDuplicates()` — the repair grade of the Gemini response policy. Returns `RepairedThemeClassifications(classifications, removedPhotoCount)`: the domain reports how much it changed, so the Vertex adapter only decides whether to log it and never diffs before/after itself |
| `application/AnalysisPipelineService.kt` | 사진 분류 후 테마별 검증·이미지 생성·업로드를 가상 스레드로 병렬 실행한다. 한 테마 안에서도 피사체 재확인과 컷아웃을 같은 executor 에서 동시에 돌리고 GCS 업로드만 양쪽을 기다려, 테마 비용이 `verify + cutout` 이 아니라 `max(verify, cutout)` 이다. 헬퍼마다 넘기던 실행·테마 인자는 `ThemeContext` 하나로 묶었다. 검증 오류는 최초 피사체·색상으로 복구한다. 테마 결과에는 명시적 피사체 없음(`012`)과 처리 오류(`013`)를 구분해 남긴다. 성공 스티커가 0개면 전부 피사체 없음일 때 `012`, 하나라도 처리 오류가 있으면 `013`로 실패하고, 하나 이상이면 성공 결과를 반환한다. `PipelineRun`이 단계 측정과 ERROR 수준의 복구 로그를 담당하며 `SimulatedProgressTicker`·`ThemeProgressEmitter`는 기존 단조 증가 진행률을 유지한다. |
| `application/PipelineRun.kt` | 실행별 단계 기록기. `measured`는 최초 실패 단계와 코드를 보존한다. 명시적으로 지정한 실패 코드를 우선하고, 그 외에는 `BusinessException`의 분석 코드 또는 `INTERNAL_ERROR`를 쓴다. `failureReason()`은 `[단계] enum 설명`만 반환해 공급자 예외 원문이 API에 노출되지 않게 한다. `degrade`는 ERROR 로그 후 테마별 복구값을 반환하고 전체 실패를 기록하지 않는다. Sentry 이슈로 수집되도록 ERROR 수준을 유지한다. `measuredGeminiCall`은 진행 중 Gemini 호출 수를 측정한다. |
| `application/AnalysisPipelineEventListener.kt` | 커밋 후 비동기 파이프라인 실행. 결과 저장 트랜잭션에서 분석 행을 다시 잠그고 `ANALYZING`일 때만 스티커 저장과 완료 처리를 함께 커밋한다. 이미 종료된 분석은 늦은 결과와 완료 푸시를 버린다. 저장 트랜잭션 실패는 `014`, 그 외에는 `PipelineRun`이 보존한 코드를 기록한다. 완료 푸시는 커밋 후, 실패 푸시는 실제 실패 전환에 성공한 경우에만 발행한다. 진행률은 `REQUIRES_NEW`로 갱신하며 진행률·푸시 오류는 `bestEffort`로 처리한다. `Semaphore(analysis.pipeline.max-concurrent-runs)`가 실행 전체를 감싸 요청이 몰려도 100장짜리 Gemini 콜과 Pixian 콜이 1.5 CPU 컨테이너에 동시에 쌓이지 않고 큐잉된다. `fallbackExecution = true`는 `OrphanedAnalysisResumeService`가 커밋할 트랜잭션이 없는 `ApplicationReadyEvent`에서 같은 리스너를 다시 태우기 위한 것이다. |
| `application/AnalysisCleanupEventListener.kt` | `@Async(AsyncConfig.ANALYSIS_CLEANUP_TASK_EXECUTOR)` + `AFTER_COMMIT` 방식의 `AnalysisDiscardedEvent` 리스너. 사용자 취소·서버 만료가 커밋된 뒤 `photoStorage.deleteAll(analysisId)`을 한 번 실행한다. `bestEffort`로 정리 실패를 기록하며 재시도하거나 이미 완료된 상태 전환을 롤백하지 않는다. |
| `application/AnalysisQueryService.kt` | Read-only analysis application boundary for active-analysis recovery, analysis status polling, and cross-domain adapters. `hasActiveAnalysis`/`getActiveAnalysis`/`getAnalysis` are single SELECTs and open no transaction. Only `ownsAnalysisPhotos(userId, boardId, analysisId, photoIds)` is `@Transactional(readOnly = true)`, because its two reads must see one snapshot; it requires the analysis to match both user and board and every photo id to belong to that analysis and board as an upload-completed photo |
| `application/PhotoQueryService.kt` | Read-only photo application boundary. `getReadablePhotos(analysisId, boardId, photoIds)` resolves upload-completed photos scoped to that analysis and board and batch-issues read signed URLs through `PhotoStorage.issueReadUrls`; `getPhotoRefs` returns the same scoped, upload-completed photo set as `PhotoRef`s whose `sourceUri` comes from `PhotoStorage.sourceUri`. Neither knows an object key. Intentionally non-transactional: the repository read is a single SELECT that commits before URL signing runs, so V4 RSA signing never holds a DB connection |
| `application/AnalysisWithdrawalService.kt` | Withdrawn-user cleanup boundary. Deletes every stored object of each analysis first (`photoStorage.deleteAll`), then hard-deletes that user's photos and analysis rows in one `TransactionTemplate` unit |
| `application/AnalysisResults.kt` | Command and result types crossing the service boundary (kept separate from the presentation dto), all on typed ids: `AnalysisCreationResult`, `PhotoUploadUrlItem`, `PhotoUploadItemRequest` (the client's per-photo request shape — `takenAt`, `contentType`, `isRepresentative`; burst-group resolution outputs `infrastructure/PhotoCreate` instead, so no request type carries a field the request never fills), `PhotoUploadGroupRequest`, `CreateAnalysisCommand`, `UploadVerificationResult`, `AnalysisStatusResult`(nullable `failedCode: AnalysisErrorCode?` 포함), `PhotoReadResult`. **Request-shape validation lives in these two `init` blocks, not in the service**: `PhotoUploadGroupRequest` rejects more than `MAX_BURST_GROUP_SIZE` (10) items with `ANALYSIS-010` and a multi-item group without exactly one representative with `ANALYSIS-009`; `CreateAnalysisCommand` rejects a group count outside `MIN_GROUP_COUNT..MAX_GROUP_COUNT` (20..100) with `ANALYSIS-001`. Both own the matching `toPhotoCreates()` — the burst-group UUID is generated there, so an unvalidated `List<PhotoUploadGroupRequest>` can never reach `AnalysisService` |
| `application/AnalysisPipelineResults.kt` | `AnalysisPipelineResult`와 `ThemeAnalysisResult`. nullable 이미지 키와 테마별 `failedCode: AnalysisErrorCode?`로 생성 성공·피사체 없음·처리 오류를 보존한다. |
| `presentation/AnalysisApi.kt` | `POST /analysis`, `GET /analysis/active`, `POST /analysis/{analysisId}/reissue`, `POST /analysis/{analysisId}/start`, `GET /analysis/{analysisId}`, and `DELETE /analysis/{analysisId}` mapping and Swagger contract, on typed ids (`UserId`, `AnalysisId`). Every failure response is a composed annotation from `AnalysisApiResponses.kt`; no `@ApiResponse(content = ...)` is restated inline and the request body schema comes from the parameter type instead of an `@RequestBody` annotation. The repeated `analysisId` path-parameter description and example are single `private const val`s in this file |
| `presentation/AnalysisController.kt` | Analysis API implementation with request binding and result mapping. Takes `@AuthenticatedUser userId: UserId` and `@PathVariable analysisId: AnalysisId` — value classes erase to `UUID`, so the shared `CurrentUserArgumentResolver` and Spring's path-variable conversion keep working unchanged |
| `presentation/AnalysisApiResponses.kt` | The domain's composed OpenAPI failure annotations: `AnalysisNotFoundApiResponse` (404 `ANALYSIS-005`), `CreateAnalysisInvalidInputApiResponse` (400), `AnalysisBoardNotFoundApiResponse` (404 `BOARD-002`, declared here rather than importing board presentation), `ActiveAnalysisExistsApiResponse` (409 `ANALYSIS-002`), `AnalysisAlreadyStartedApiResponse` (409 `ANALYSIS-003`), `AnalysisStartConflictApiResponse` (409 `ANALYSIS-003`/`008`), `AnalysisCancelNotAllowedApiResponse` (409 `ANALYSIS-004`). Each names the error codes that endpoint can actually produce |
| `presentation/AnalysisApiExamples.kt` | `ApiExampleProvider` implementation. Defines analysis creation request/response, per-status (ANALYZING/COMPLETED/FAILED) polling responses with a typed failure-code example, the no-analysis-in-progress response, the reissued-upload-urls response, the upload confirmation response, the empty-success cancel response, and the `ANALYSIS-001`/`002`/`003`/`004`/`005`/`008` plus propagated `BOARD-002` failure examples as real DTO instances. To respect the domain boundary, the `BOARD-002` example lives here via `ApiExamples.errorExample` instead of importing board presentation |
| `presentation/dto/` | `AnalysisStatusResponse.failedCode`는 nullable 도메인 enum이며 JSON에서는 코드 문자열로 내려간다. HTTP 200·성공 envelope는 상태 조회 성공을 뜻하고 분석 결과는 `data.status`로 판별한다. 기존 원인 불명 실패는 코드를 추정하지 않고 null을 유지한다. Swagger-described request/response schemas, all id fields typed (`BoardId`, `AnalysisId`, `PhotoId`; the JSON representation stays a plain uuid string). `CreateAnalysisRequest`'s `@ArraySchema` description states the 20–100 group / 1–10 photo-per-group bounds as text only — the one enforcement point is the `init` of `CreateAnalysisCommand` (`MIN_GROUP_COUNT`/`MAX_GROUP_COUNT`) and `PhotoUploadGroupRequest` (`MAX_BURST_GROUP_SIZE`), reached through `CreateAnalysisRequest.toCommand()`, so the DTO does not re-validate or reference those constants. `PhotoUploadItem` takes `contentType` as the request-only `PhotoUploadContentType` enum so new uploads allow JPEG/PNG/WEBP while existing HEIC rows remain readable through the domain enum; non-null properties carry no `@field:NotNull` because Jackson already rejects a missing value with 400. `CreateAnalysisResponse` and `ReissueUploadUrlsResponse` share one `PhotoUploadUrlResponse` with a `from` factory, so no import alias is needed against the application-layer `PhotoUploadUrlItem` |

## Rules

- Typed identifiers (`AnalysisId`, `PhotoId`, `BoardId`, `UserId`) run end to end through this package: controller, DTOs, application services, `Analysis`/`Photo` models, the `PhotoStorage`/`ThemeClassifier` ports, the `integration/` adapters, and the public signatures of `AnalysisRepository`/`PhotoRepository`. jOOQ id columns are generated typed (codegen `forcedType`), so DSL bindings and `toDomain` take and return typed ids directly — there is no wrap/unwrap layer left in this domain. Raw `UUID` remains only where it is not an entity id: `photos.burst_group_id` (an internal grouping token that crosses no domain boundary) and the sticker regeneration id.
- This tracks the client's real OpenAPI spec (`analysis` tag). Implemented: the 20–100 photo group count check, each group 1–10 photos (`ANALYSIS-001`, enforced in `CreateAnalysisCommand.init`, returning 400), the one-active-analysis-per-user limit (`ANALYSIS-002`, service pre-check + partial unique index `uk_analysis_active` returning 409 on violation), already-started/finished conflict (`ANALYSIS-003`, 409), the `UPLOADING`-only cancel (`ANALYSIS-004`, 409, `DELETE /analysis/{id}`), active-analysis recovery, analysis status polling, upload URL reissue (`POST /analysis/{id}/reissue`), and AI pipeline start from `start`. Not implemented yet: the daily quota (out of scope for this PR — to be implemented later). Not-found cases use `NotFoundException(ANALYSIS-005)` for missing analysis and ownership mismatch in query APIs, including `cancelAnalysis` and `startUpload`/`reissueUploadUrls` (both share `findOwnedAnalysis`, which consistently throws `ANALYSIS-005` on owner mismatch instead of a generic 404).
- `cancelAnalysis` only accepts `UPLOADING` analysis, same restriction as `startUpload`, so a canceled analysis can never race a pipeline already in flight — by the time an analysis reaches `ANALYZING` its photos are the ones `startUpload` already locked in. On success it hard-closes the session (`FAILED`/`ANALYSIS-016`/`CANCELED` + all photos `FAILED`) rather than deleting rows, so the client's create-analysis flow can immediately retry (the partial unique index no longer blocks it) while the canceled session remains visible in history like any other failure. GCS cleanup is asynchronous and best-effort (see `AnalysisCleanupEventListener` above); nothing currently retries a failed `deleteByPrefix`, so a cancel can legitimately leave orphaned objects in storage on delete failure — same trade-off already accepted by `AnalysisWithdrawalService`. A future batch job to sweep and re-delete orphaned prefixes for `FAILED` analyses is not implemented; that gap is recorded here rather than as a comment in the listener.
- Analysis creation and upload verification require the authenticated principal (`UserId`). Foreign board and analysis identifiers return not found without exposing ownership.
- Board access goes through the port-free `BoardAccessService` only, never `BoardQueryService` or a board repository. `createAnalysis` takes the target board row lock (`getOwnedByIdForUpdate`) before the active-analysis pre-check, so it serializes against board deletion and a concurrent delete makes creation fail with `BOARD-002` instead of leaving an active analysis on a deleted board. That lookup is `MANDATORY`-propagation, so it may only be called from inside an already-open transaction.
- `AnalysisStatus.ACTIVE` (`UPLOADING`, `ANALYZING`) is the single definition of "in progress" shared by the active-analysis pre-check, active-analysis recovery, and the board deletion guard. Keep it aligned with the `uk_analysis_active` partial unique index predicate and the status transitions in `docs/api-spec/api-spec.md` and `docs/erd/schema.dbml`.
- Board deletion does not touch finished (`COMPLETED`/`FAILED`) analysis rows: the spec defines no deletion policy for analysis sessions, so the history stays and keeps pointing at the soft-deleted board.
- `startUpload` only accepts `UPLOADING` analysis. It checks photos currently `PENDING`; GCS objects with `size > 0` become `COMPLETED`, while missing or 0-byte objects become `FAILED` and are excluded from the pipeline. The analysis transitions to `ANALYZING`, so calling `startUpload` again returns `ANALYSIS-003` instead of acting as a no-op. The `UPLOADING` check runs **twice** through `Analysis.requireUploading()`: once on the unlocked read before the GCS listing, and again inside the transaction right after `findByIdForUpdate`, so a request that passed the first check while another `startUpload` was in flight still fails with `ANALYSIS-003` and marks nothing `COMPLETED`. If no pending photo has a non-empty object, nothing is written and the analysis stays `UPLOADING` with `ANALYSIS-008` (`NO_UPLOADED_PHOTOS`), so the client can upload and retry.
- `upload_status`/`status`/`content_type` are DB `VARCHAR`, not Postgres `ENUM`s — value-set validation lives only in the Kotlin enums; repositories map `String ↔ enum` at the boundary (`PhotoContentType.entries.find { it.mimeType == ... }` / `.mimeType`, same pattern as `UploadStatus.valueOf(...)` / `.name`).
- This domain owns the sticker domain's analysis/photo integration adapters (`infrastructure/integration/Sticker*`), following the same "provider side owns the adapter" rule as `BoardAnalysisActivityAdapter`. Sticker code never reads `analysis` or `photos` directly; every cross-domain read goes through `AnalysisQueryService` / `PhotoQueryService`.
- Ownership checks are fail-closed: a missing analysis, a user/board mismatch, or any photo id outside the analysis or not upload-completed returns `false`, and the sticker domain converts that into `STICKER-001`. The pipeline never trips the COMPLETED requirement because `startUpload` publishes only photos it just marked `COMPLETED`, and the recap read path fails fast (500) instead of silently dropping a non-completed photo a recap references.
- Recap photo reads require `photos.taken_at`; a null value is a data-integrity bug and fails with `IllegalStateException` (500) rather than being silently dropped.
- The paid/destructive external adapters of this domain (`PhotoStorage`, `ThemeClassifier`, `StickerGenerator`, `StickerStorage`) are replaced by `@Primary` fakes from `analysis/support/AnalysisTestConfig`, which the shared `support/IntegrationTest` base class imports for every integration test. No test opts in with its own `@Import`, so a new test cannot forget it and hit real GCS or Vertex AI. `FakePhotoStorage` deliberately delegates `issueReadUrls` to the real `GcsReadUrlIssuer`: read-URL signing is local RSA against the dummy test key with no network call and no cost, and keeping it real is what lets recap tests assert the exact signed object key.
- All four fakes are `ResettableFake`, and `support/IntegrationTest` truncates the DB and resets every one of them before each root `Given`. Never add a per-spec `beforeSpec`/`afterEach` reset — it is redundant and hides which leaf actually set the state.
- **`FakePhotoStorage.issueUploadUrls` must never register an uploaded object.** Signing a PUT URL does not create the object in GCS, so a fake that registers one makes `ANALYSIS-008` unreachable and every `startUpload` spec pass without a single upload. A spec that wants a photo present calls `markUploaded(photo)` / `markUploaded(photos)` (optionally `size = 0` for the empty-body case) and `markMissing(photo)` to take one away. The failure knobs are `issueUploadUrlsFailure` (pins "URL signing happens after the create transaction commits"), `deleteAllFailure` (pins the best-effort cancel cleanup), and `onUploadedObjects`, a hook that runs inside `startUpload` between the unlocked status check and the row lock — that is the seam the post-lock `ANALYSIS-003` test uses. `FakeStickerGenerator.onGenerate` (receives the `targetSubject`, so one theme can fail while others succeed) and `FakeStickerStorage.uploadFailure` 는 초기 파이프라인의 테마별 실패·전체 실패(`013`)와 재생성 API의 `011`을 검증한다.
- The listener's "save stickers and `markCompleted` in one transaction" rule is pinned by adding a temporary `CHECK (status <> 'COMPLETED')` constraint on `analysis` around one `handle` call: `markCompleted` then fails after the stickers were written, and the spec asserts zero stickers survive. Do not replace that with a production seam — the constraint is created and dropped inside the one `When`.
- Photo objects and generated sticker objects share one bucket but not one key space: `PhotoObjectKeys` owns `photos/{analysisId}/...` and `StickerObjectKeys` owns `stickers/{analysisId}/...` for initial output plus `stickers/{stickerId}/{sourcePhotoId}-{regenerationId}.png` for regeneration output. Uploads (`GcsPhotoStorage`, `GcsStickerStorage`) live here; read URL signing for both is the shared `global/storage/GcsReadUrlIssuer`, reached through `PhotoStorage.issueReadUrls` for photos and through the sticker domain's `StickerImageStoragePort` for stickers.
- `AnalysisStickerResult.imageKey` must receive the bare object key from `StickerObjectKeys.keyFor(...)` or `StickerObjectKeys.keyForRegeneration(...)`, never a `gs://` URI — the latter would be signed as a literal object name and produce a broken read URL.
- **`UPLOADING` / `ANALYZING` 에 멈춘 분석은 만료 배치가 정리한다.** `uk_analysis_active` 가 사용자당 진행 중 분석을 1개로 강제하므로, 파이프라인이 죽거나 사용자가 업로드 도중 이탈해 남은 행은 그 사용자의 새 분석을 **영구히** 409 `ANALYSIS-002` 로 막는다. 프로덕션에서 26일째 막혀 있던 사용자 4명이 이 배치를 만든 이유이고, 4명 모두 성공한 분석이 0건이었다.
- 만료 기준은 `created_at` 이 아니라 **`updated_at`** 이다. `analysis_set_updated_at` 트리거가 모든 UPDATE 에서 `updated_at` 을 갱신하므로, 진행률이 오르고 있는 살아 있는 `ANALYZING` 은 계속 신선해지고 죽은 것만 오래된 상태로 남는다. `created_at` 을 쓰면 오래 걸리는 정상 분석까지 죽인다. 그래서 테스트에서 오래된 행을 만들려면 그 statement 동안만 트리거를 꺼야 한다 — `StaleAnalysisCleanupServiceTest.ageTo` 가 그 방법이다.
- 만료와 사용자 취소는 `AnalysisDiscardService.discard(analysisId, failedReason, failedCode)`를 공유한다. 취소는 `016/CANCELED`, 만료는 `015/EXPIRED`이며 사진 실패 처리와 `AnalysisDiscardedEvent` 발행은 동일하다. 사진 오브젝트 정리는 커밋 후 비동기로 실행한다.
- 만료는 푸시를 보내지 않는다. 한 시간 넘게 방치된 분석의 실패 알림은 사용자가 이미 화면을 떠난 뒤에 도착하는 소음이고, 이 배치의 목적은 통보가 아니라 점유 해제다.
- 배치는 `ANALYSIS_STALE_CLEANUP_ENABLED`로 켜고 끄며 `.env.template` 기본값은 켜짐, 타임아웃 60분, 10분 간격 크론이다. 분석·사진 행을 삭제하지 않고 실패 처리한 뒤 업로드 오브젝트를 정리한다. 테스트 프로파일에서는 스케줄러를 끄고 `StaleAnalysisCleanupService`를 직접 호출한다.
- Gemini response policy has three grades and no more. **Repair** (`domain/RepairedThemeClassifications.kt`): a photo id appearing in more than one theme's `categorizedPhotoIds`. **Fail the analysis** (`INVALID_GEMINI_RESPONSE`, `ANALYSIS-007`): a blank `theme`/`recap.badge`/`recap.text`/`stickerTargetSubject`, an out-of-set or duplicated `stickerSourcePhotoId`, a photo id outside the request, a photo id repeated inside one theme, and a theme count below `MIN_THEME_COUNT`. **Degrade silently** (log a warning, keep the analysis): only `mainColor` and `comments`, per the next rule.
- **뱃지·요약의 길이 초과는 세 등급 중 어디도 아닌 어댑터 경계의 절삭 정규화다.** `VertexAiGeminiClassifier.cappedText`가 `recap.badge`를 `RecapContent.MAX_BADGE_LENGTH`(15), `recap.text`를 `MAX_TEXT_LENGTH`(100)로 자르고 WARN을 남긴다. 이 두 숫자는 `sticker/domain/Sticker.kt`의 `MAX_TITLE_LENGTH`/`MAX_SUMMARY_LENGTH`와 **의도적으로 중복**이다 — 크로스 도메인 상수 import 대신, `VertexAiGeminiClassifierSchemaTest`가 잘린 값을 `Sticker.isValidTitle`/`isValidSummary`로 단언해 두 숫자가 어긋나면 테스트가 깨진다. 실패(`007`)가 아닌 이유는 blank와 달리 앞부분이 그대로 쓸 수 있는 값이기 때문이고, degrade가 아닌 이유는 `mainColor`의 `#222222` 같은 의미 있는 기본값이 뱃지에는 없기 때문이다. 가드가 없으면 `Sticker`의 `require`가 파이프라인 맨 끝에서 터져 75초짜리 분석과 사진 100장이 통째로 버려진다 — repair 등급을 도입한 것과 같은 비용 구조다. **네 번째 등급을 만들지 말 것.**
- `classifyAndRecap` still calls `ThemeClassificationValidator.validate` unconditionally, and repair runs **before** it, never instead of it. Repair must never be allowed to make validation reachable-but-toothless: it must never be guarded by `if (classifications.isNotEmpty())`, which would make `MIN_THEME_COUNT` unreachable and let a response with no usable theme finish as a `COMPLETED` analysis holding zero stickers.
- **The repair grade is a reversal of the earlier "exactly two grades" rule. Do not revert it.** Cross-theme duplication was the single largest cause of failed analyses in production — 13 of 33 non-cancelled failures, and the only cause still occurring after 2026-08-19. It is a 200 OK whose schema is valid and whose meaning is wrong, so the SDK's `HttpRetryOptions` (status codes only) never retried it; roughly 75 seconds of classification was thrown away and the user had to re-upload every photo. Repair is safe here because only one photo's ownership is ambiguous — every other field of the response is usable. Alias-level repair already existed in `toClassifications` (unknown aliases are dropped), so this is the same shape one layer up, not a new kind of leniency.
- **테마 개수 초과는 실패가 아니라 절삭이다** (`cappedToMaxThemeCount`, 중복 교정 바로 앞). 응답 스키마에 `minItems`/`maxItems` 를 걸어 Gemini 쪽에서 먼저 막지만, 구조화 출력이 그것을 어겨도 이미 성공한 100장짜리 분류 콜(가장 비싼 단계)을 개수 하나 때문에 버리지 않는다. 앞에서부터 `MAX_THEME_COUNT` 개만 남기며, 잘려나간 테마의 사진은 그냥 미분류로 남는다 — 프롬프트가 이미 "어느 테마에도 맞지 않는 사진은 빼도 된다"고 허용한 상태다. **하한은 그대로 실패다**: 테마가 0개면 살릴 결과 자체가 없다.
- **테마 개수 초과는 실패가 아니라 절삭이다** (`cappedToMaxThemeCount`, 중복 교정 바로 앞). 응답 스키마에 `minItems`/`maxItems`를 걸어 Gemini 쪽에서 먼저 막지만, 구조화 출력이 그것을 어겨도 이미 성공한 100장짜리 분류 콜(가장 비싼 단계)을 개수 하나 때문에 버리지 않는다. 앞에서부터 `MAX_THEME_COUNT`개만 남기며, 잘려나간 테마의 사진은 그냥 미분류로 남는다 — 프롬프트가 이미 "어느 테마에도 맞지 않는 사진은 빼도 된다"고 허용한 상태다. **하한은 그대로 실패다**: 테마가 0개면 살릴 결과 자체가 없다.
- `repairCrossThemeDuplicates` gives the theme that names the photo as its `stickerSourcePhotoId` priority over the theme that merely mentions it first. Keeping "first theme wins" would strip a later theme's sticker source and fail `validateStickerSourcePhotoId` anyway, so a naive repair would trade one failure for another. When two themes claim the same photo as their source, or when repair empties a theme, the response is genuinely unrepairable and validation still fails it with `ANALYSIS-007` — `RepairedThemeClassificationsTest` pins all four of these cases.
- `AnalysisStickerResult.mainColor` / `ThemeAnalysisResult.stickerMainColor` comes from Gemini's classification response (`sticker.mainColor`). Unlike the fail-the-analysis fields above, a missing or malformed `mainColor` does not throw `INVALID_GEMINI_RESPONSE` and does not fail the whole analysis — `VertexAiGeminiClassifier.sanitizedMainColor` degrades it to a fixed default instead, since it is cosmetic (recap-screen background color) and not worth discarding an otherwise-valid analysis over. `regenerateSticker()` applies the same sanitization, so `POST /stickers/{stickerId}/regenerate` also refreshes `stickers.main_color` alongside the image.
- `ThemeAnalysisResult.text` is the prompt's "한 문장의 리캡 문구" and fills `AnalysisStickerResult.summary` (the recap's `한 줄 요약`), not a recap comment. It used to be saved as the single non-floating comment, but that "하단 순차 노출 문장형" UI was removed. `Sticker.MAX_SUMMARY_LENGTH` is 100, well above what a single sentence yields, so keep the prompt's length wording in sync if either side changes.
- `ThemeClassification.comments` / `ThemeAnalysisResult.comments` (`analysis/domain/ThemeComment.kt`: `content`, `posX`, `posY`) come from the same Gemini call as `recap`/`sticker`, requested as two separate arrays (`comments.speechBubbles` with required `posX`/`posY`, `comments.keywordChips` as bare strings) so the structured-output schema can't produce a half-filled coordinate pair — `VertexAiGeminiClassifier.sanitizedComments` then flattens both into `List<ThemeComment>`, dropping any speech bubble missing content or a coordinate, any blank keyword chip, and any bubble or chip longer than `ThemeComment.MAX_BUBBLE_LENGTH`/`MAX_CHIP_LENGTH` (20). 길이 상한의 근거는 클라이언트 `RecapStickerVisual.tsx`의 `posX = side * min(idealX, max(0, maxX))`, `maxX = 176 - 8 - width / 2`다 — 말풍선 실측 폭이 336px(14px 기준 약 23자)에 이르면 `posX`가 0이 되어 스티커 정중앙을 덮는다. 잘리지도 줄바꿈되지도 않고 겹침으로 실패하므로, 화면에 담을 수 없는 길이는 서버에서 버리는 쪽이 낫다 (same graceful-degradation pattern as `sanitizedMainColor`; a bad/empty `comments` response never fails the whole classification). `AnalysisPipelineEventListener.toStickerResult()` maps each `ThemeComment` 1:1 into a `RecapCommentCreation` (`posX`/`posY` both null for a keyword chip, both set for a speech bubble), matching `RecapComment`'s all-or-nothing position rule.
- `GcsSourcePhotoImageReader`, `PixianBackgroundRemover`, `StickerImageCropper`는 인프라 예외를 그대로 전파한다. 초기 파이프라인은 테마별 실패로 보존한 뒤 전체 생성 성공이 0개면 `ANALYSIS-013`로 종료하고, 재생성 API는 `AnalysisStickerRegenerationAdapter`에서 기존 `ANALYSIS-011`로 변환한다. Gemini 분류 어댑터는 호출 실패 `017`과 응답 오류 `007`을 구분한다.
- **재확인(verify)과 스티커 컷아웃을 병렬로 돌리는 대신 Pixian 크레딧을 버린다.** 재확인이 "피사체 없음"으로 판정하면 이미 만들어진 컷아웃은 업로드하지 않고 버리며(`uploadedStickerKey`가 WARN을 남긴다), 그만큼 크레딧이 낭비된다. 직렬로 되돌리면 낭비는 사라지지만 테마마다 Gemini 왕복 하나가 다시 크리티컬 패스에 들어온다. 이 교환이 남는 장사인지는 WARN 로그의 발생 빈도로 판단할 것 — 판정 실패율이 높으면 `processTheme`에서 `verification.getOrThrow()`를 `generateStickerImage` 앞으로 옮기는 한 줄이 되돌리는 방법이다. 컷아웃은 `targetSubject`를 쓰지 않고(로그에만 남는다) 배경만 제거하므로, 병렬로 돌려도 만들어지는 이미지 자체는 직렬일 때와 같다.
- **재시작으로 끊긴 분석은 만료를 기다리지 않고 부팅 때 재개한다** (`OrphanedAnalysisResumeService`). 파이프라인 상태는 `analysis-pipeline-` 가상스레드 안에만 있고 `stop_grace_period`는 30초인데 파이프라인은 분 단위라, 배포 한 번에 그 순간 `ANALYZING`인 건이 전부 죽는다. 재개가 없으면 사용자는 만료 시한(기본 60분)이 지나 크론이 `EXPIRED`로 바꿀 때까지 로딩 화면에 남는다. 재개에 필요한 것은 전부 DB에 있다 — `photos`는 `COMPLETED`, `analysis`는 `ANALYZING`. 앱 인스턴스가 **한 대**(`compose.production.yaml`의 단일 `app` 서비스)라서 중복 재개가 없다는 전제 위에 서 있다. 인스턴스를 늘리는 순간 이 서비스에 소유권(instance id + heartbeat)이 필요해진다.
- 재개 대상은 `updated_at`이 만료 시한 **안쪽**인 `ANALYZING`만이다. 그보다 오래된 것은 이미 만료 배치의 몫이고, 그 경계를 지켜야 크래시 루프가 같은 분석의 Gemini·Pixian 호출을 무한 반복하지 않는다. `resume-limit`가 한 번의 부팅에서 재개할 건수를 추가로 묶는다.
- `updateProgress`는 `progress < :new` 조건을 달아 **되돌아가지 않는다**. 테마별 진행률 emitter는 단조 증가 값을 내지만 그 값을 쓰는 `REQUIRES_NEW` 트랜잭션들은 순서를 보장하지 않아 늦게 도착한 낮은 값이 앞선 값을 덮을 수 있었고, 재개된 분석은 45부터 다시 올라오기 때문에 조건이 없으면 클라이언트에서 진행률이 뒤로 간다.
- Withdrawn-user cleanup deletes stored objects before rows, so a failure after object deletion leaves rows that the next batch run retries; deleting rows first would leak objects nothing can find again. It is the only path that hard-deletes analysis and photo rows.
- **Service bodies in `application/` carry no logging, timing, best-effort swallowing, or retry.** Pipeline steps go through the `PipelineRun` recorder (`measured` / `degrade` / `measuredGeminiCall`), side effects that must not fail the caller go through `global/logging`'s `bestEffort(log, "…") { }`, and retry goes through `global/retry`'s `retrying(...)`. `grep -n "log\\.\\|nanoTime\\|elapsedMs\\|runCatching" application/*Service*.kt application/*Listener*.kt` must stay empty — `PipelineRun.kt` is the one file in this package allowed to hold them. No `@Aspect` for the pipeline: its timed units are internal steps inside one method, not bean method boundaries, so a proxy cannot see them (CLAUDE.md 2.2).
- **Request-shape validation belongs to the command types, not to `AnalysisService`.** A new group/count rule is an `init` check on `CreateAnalysisCommand` or `PhotoUploadGroupRequest`; a new analysis-state rule is a method on `Analysis` next to `requireUploading()`. Because the rules fire in the constructor, a test that asserts `ANALYSIS-001`/`ANALYSIS-009`/`ANALYSIS-010` must build the command **inside** `shouldThrow`, not in the `Given` block.
- The old `images` table / `image` domain (file-name-based, per-image signed URL issuance and client-reported upload completion) was fully replaced by this domain in the same change — see git history if you need the old shape.

Update this file when layers are added to this domain.
